Bob and Jeremy:

* see if CDF_INT8 variable listed at http://mag.gmu.edu/git-data/cdawmeta/data/hapi/errors/HAPI.NotImplemented.log can be represented as 32-bit ints or 64-bit doubles (between +/- 2^52) by looking at the VALID_MAX and VALID_MIN. Jeremy will look into how Nand handled these (I think Nand drops).
